# Unpublished 0.4.0-dev

This tree is **not** the Official Registry / npm listing surface.

- **npm 0.3.1** (when published) is a metadata-only listing tarball cut from the real 0.3.0 tree (`d70470b`). Same runtime as 0.3.0; no `dqla_`.
- **This branch** is unpublished `0.4.0-dev` and continues the `dqla_` / `DQL_ACCOUNT_TOKEN` / `X-DQL-Account` path.
- Do not treat the main README as a published `dqla_` release. Developers: see the clearly marked unpublished section in [README.md](./README.md).

Do not publish this branch.

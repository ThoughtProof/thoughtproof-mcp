{
  "$schema": "https://glama.ai/mcp/schemas/server.json",
  "maintainers": ["ThoughtProof"],
  "name": "thoughtproof-mcp",
  "description": "Pre-action verification gate for autonomous agents: verify before they pay, trade, write or deploy. Hero tools verify_before_action / verify_decision return ALLOW|BLOCK|UNCERTAIN + execute flag + receipt. Soft fail-closed: execute is true only on ALLOW.",
  "type": "mcp_server",
  "status": "stable",
  "framework": "@modelcontextprotocol/sdk",
  "tools": 4,
  "transport": ["stdio"],
  "keywords": [
    "verification",
    "pre-action-gate",
    "verify-before-action",
    "ai-safety",
    "agent-guard",
    "fail-closed",
    "decision-verification",
    "mcp-server",
    "x402",
    "dql",
    "sentinel",
    "base"
  ],
  "features": {
    "adversarial_verification": {
      "enabled": true,
      "note": "4 LLMs challenge each claim independently"
    },
    "trust_scores": {
      "enabled": true,
      "note": "Agent trust score lookup via ERC-8004"
    },
    "x402_payment": {
      "enabled": true,
      "note": "Pay-per-verification via USDC on Base"
    }
  }
}

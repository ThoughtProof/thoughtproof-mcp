# Example env for local MCP hosts. Do not commit real keys.
# Hero verify key uses prefix dqlk_ (not account token prefix dqla_).

DQL_API_KEY=
SENTINEL_API_KEY=
THOUGHTPROOF_API_KEY=
# DQL_SANDBOX=1
